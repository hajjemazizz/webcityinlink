framework:

    default_locale: en
    translator:
        default_path: '%kernel.project_dir%/translations'
        fallbacks:
            - en
#        providers:
#            crowdin:
#                dsn: '%env(CROWDIN_DSN)%'
#            loco:
#                dsn: '%env(LOCO_DSN)%'
#            lokalise:
#                dsn: '%env(LOKALISE_DSN)%'

    
    # Assurez-vous que le reste de votre configuration framework est ici et est correct.
    # Par exemple, la configuration de la session, des formulaires, etc.

