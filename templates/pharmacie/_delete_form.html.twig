<form method="post" action="{{ path('app_pharmacie_delete', {'idPharmacie': pharmacie.idPharmacie}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pharmacie.idPharmacie) }}">
    <button class="btn">Delete</button>
</form>
